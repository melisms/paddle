<% if current_user %>
   <div class="menu">
        <a href="#">🏠 Home</a>
        <a href="#">🐾 Profile</a>
        <a href="#">💌 Messages</a>
        <a href="#">⚙ Settings</a>
        <%= link_to "🔓 Logout", destroy_user_session_path, method: :delete %>
    </div>
<% else %>
    <div class="menu">
        <a href="#">🏠 Home</a>
        <a href="#">🐾 Profile</a>
        <a href="#">💌 Messages</a>
        <a href="#">⚙ Settings</a>
    </div>
    <div class="auth">
        <button onclick="window.location.href='/users/sign_up'">Sign Up</button>
        <button onclick="window.location.href='/users/sign_in'">Log In</button>
    </div>
<% end %>
