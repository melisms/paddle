
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            gap: 20px; 
            padding: 20px;
            background-color: rgb(210, 188, 156);
        }

        .left-section {
            flex: 1;
            max-width: 500px;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .right-section {
            flex: 1;
            max-width: 500px; 
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        .right-section img {
            max-width: 100%;
            height: auto; 
            border-radius: 10px;
        }

        .field {
            position: relative;
        }

        .field, .actions {
            margin-bottom: 15px;
        }

        .field label {
            display: block;
            font-size: 14px;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .field input {
            width: 100%;
            padding: 10px 10px 10px 35px; 
            border: 1px solid #ced4da;
            border-radius: 5px;
            font-size: 14px;
            background-color: #f9f9f9;
        }

        .actions {
            display: flex;
            flex-direction: column;  
            align-items: center;     
            justify-content: center; 
            gap: 10px;               
        }

        .actions input.login-btn {
            background-color: #00796b;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 200px;
        }

        .actions input.login-btn:hover {
            background-color: #005b4f;
        }

        .actions .signup-btn {
            background-color: #00796b;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            width: 200px;
            text-align: center;
        }

        .actions .signup-btn:hover {
            background-color: #005b4f;
        }

        .left-section .remember-me {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            margin-top: 10px;
            margin-bottom: 10px;
        }

        .left-section .remember-me input {
            margin-right: 5px;
	    }

        .alert {
            padding: 10px;
            background-color: #f8d7da;
            color: #721c24;
            border-radius: 5px;
            margin-bottom: 15px;
        }


    </style>
</head>
<body>
    <div class="container">
        <div class="left-section">
            <% if flash[:alert] || flash[:notice] %>
                <div class="alert">
                    <%= flash[:alert] || flash[:notice] %>
                </div>
            <% end %>

            
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                <div class="field">
                    <label for="email">Email</label>
                    <div style="position: relative;">
                        <i class='bx bx-user' style="position: absolute; left: 10px; top: 50%; transform: translateY(-50%); font-size: 20px; color: #aaa;"></i>
                        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
                    </div>
                </div>

                <div class="field">
                    <label for="password">Password</label>
                    <div style="position: relative;">
                        <i class='bx bx-lock' style="position: absolute; left: 10px; top: 50%; transform: translateY(-50%); font-size: 20px; color: #aaa;"></i>
                        <%= f.password_field :password, autocomplete: "current-password" %>
                    </div>
                </div>

                <% if devise_mapping.rememberable? %>
                    <div class="remember-me">
                        <%= f.check_box :remember_me %>
                        <%= f.label :remember_me %>
                    </div>
                <% end %>

                <div class="actions">
                    <%= f.submit "Log in", class: "login-btn" %>
                    <%= link_to "Sign up", new_registration_path(resource_name), class: "signup-btn" %><br />
                </div>
            <% end %>

            <%= render "devise/shared/links" %>
        </div>

        <div class="right-section">
            <img src="<%= asset_path('dogs.jpg') %>" alt="Dog Image">
        </div>
    </div>
</body>
</html>
