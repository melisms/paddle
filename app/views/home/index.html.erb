<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Paddle</title>
  <style>
      body {
          margin: 0;
          font-family: Arial, sans-serif;
          background-color: #f0f8f0;
          display: flex;
          justify-content: center;
      }

      .layout {
          display: flex;
          width: 100%;
          max-width: 1600px;
          height: 100vh;
      }

      .header {
          width: 100%;
          background-color: #228b22;
          color: white;
          padding: 15px 20px;
          display: flex;
          justify-content: space-between;
          align-items: center;
      }

      .header .logo {
          font-size: 24px;
          font-weight: bold;
          letter-spacing: 2px;
      }

      .header .logo span {
          color: #fff;
          font-size: 26px;
          font-weight: 600;
      }

      .left-bar {
          width: 30%;
          background-color: #ffffff;
          border-right: 1px solid #ddd;
          padding: 20px;
          box-sizing: border-box;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
      }

      .left-bar .menu {
          display: flex;
          flex-direction: column;
          gap: 20px;
      }

      .left-bar a {
          text-decoration: none;
          color: #228b22;
          font-size: 18px;
          font-weight: bold;
          transition: 0.3s ease;
      }

      .left-bar a:hover {
          color: #1c6b1c;
      }

      .left-bar .auth {
          margin-top: 30px;
          display: flex;
          flex-direction: column;
          gap: 15px;
      }

      .left-bar button {
          padding: 10px 20px;
          background-color: #228b22;
          color: white;
          border: none;
          border-radius: 5px;
          font-size: 16px;
          cursor: pointer;
          transition: 0.3s ease;
      }

      .left-bar button:hover {
          background-color: #1c6b1c;
      }

      .main-content {
          flex-grow: 1;
          background-color: #ffffff;
          border-left: 1px solid #ddd;
          border-right: 1px solid #ddd;
          padding: 20px;
          overflow-y: auto;
      }

      .post-input {
          padding: 20px;
          border-bottom: 1px solid #ddd;
          position: relative;
      }

      .post-input textarea {
          width: 100%;
          padding: 10px;
          border: 1px solid #ddd;
          border-radius: 5px;
          resize: none;
          font-size: 16px;
      }

      .post-input button {
          margin-top: 10px;
          background-color: #228b22;
          color: white;
          border: none;
          padding: 10px 20px;
          border-radius: 5px;
          cursor: pointer;
          transition: 0.3s ease;
      }

      .post-input button:hover {
          background-color: #1c6b1c;
      }

      .char-count {
          position: absolute;
          bottom: 10px;
          right: 10px;
          font-size: 14px;
          color: #555;
      }

      .posts-feed {
          padding: 20px;
      }

      .post {
          border: 1px solid #ddd;
          border-radius: 10px;
          padding: 15px;
          margin-bottom: 15px;
          background-color: #eaf8e9;
      }

      .post-header {
          font-weight: bold;
          margin-bottom: 5px;
          color: #228b22;
      }

      .post-time {
          font-size: 12px;
          color: #555;
      }

      .right-bar {
          width: 35%;
          background-color: #ffffff;
          border-left: 1px solid #ddd;
          padding: 20px;
          box-sizing: border-box;
      }

      .search-bar {
          display: flex;
          align-items: center;
          gap: 10px;
      }

      .search-bar input {
          width: 100%;
          padding: 10px;
          border: 1px solid #ddd;
          border-radius: 5px;
      }

      .search-bar button {
          padding: 10px 20px;
          background-color: #228b22;
          color: white;
          border: none;
          border-radius: 5px;
          font-size: 16px;
          cursor: pointer;
      }

      .search-bar button:hover {
          background-color: #1c6b1c;
      }

      .explore {
          margin-top: 20px;
      }

      .explore h3 {
          margin-bottom: 10px;
          color: #228b22;
      }

      .explore-item {
          margin-bottom: 10px;
          padding: 10px;
          border: 1px solid #ddd;
          border-radius: 5px;
          background-color: #f5f5f5;
      }

      .explore-item:hover {
          background-color: #e6f7ff;
          cursor: pointer;
      }

      .trending {
          margin-top: 20px;
      }

      .trending h3 {
          margin-bottom: 10px;
          color: #228b22;
      }

      .trend-item {
          margin-bottom: 10px;
          padding: 10px;
          border: 1px solid #ddd;
          border-radius: 5px;
          background-color: #f5f5f5;
      }

      .trend-item:hover {
          background-color: #e6f7ff;
          cursor: pointer;
      }

  </style>
</head>
<body>

<div class="header">
  <div class="logo">
    Paddle
  </div>
</div>

<div class="layout">

  <div class="left-bar">
    <%= render 'user/session_manager' %>
  </div>

  <div class="main-content">
    <div class="post-input">
      <textarea id="postContent" rows="3" placeholder="Share your pet's moments..." maxlength="350" oninput="updateCharCount()"></textarea>
      <div class="char-count" id="charCount">0/350</div>
      <button onclick="addPost()">Post</button>
    </div>
    <div class="posts-feed" id="postsFeed"></div>
  </div>

  <div class="right-bar">
    <div class="search-bar">
      <input type="text" placeholder="Search pets or hashtags...">
      <button onclick="alert('Search clicked')">Search</button>
    </div>

    <div class="explore">
      <h3>Explore</h3>
      <div class="explore-item">🐶 #CuteAnimals</div>
      <div class="explore-item">🐱 #PetAdoption</div>
      <div class="explore-item">🐾 #PetsOfTheDay</div>
    </div>

    <div class="trending">
      <h3>Trending Topics</h3>
      <div class="trend-item">🐕 #DogLovers</div>
      <div class="trend-item">🐈 #CatVideos</div>
      <div class="trend-item">🐦 #BirdWatching</div>
    </div>
  </div>
</div>

<script>
    function addPost() {
        const postContent = document.getElementById('postContent').value.trim();
        if (postContent === '') {
            alert('Please share your pet moment!');
            return;
        }

        const postElement = document.createElement('div');
        postElement.className = 'post';

        const currentTime = new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });

        postElement.innerHTML = `
        <div class="post-header">User's Pet</div>
        <div>${postContent}</div>
        <div class="post-time">${currentTime}</div>
      `;

        const postsFeed = document.getElementById('postsFeed');
        postsFeed.prepend(postElement);

        document.getElementById('postContent').value = '';
        document.getElementById('charCount').textContent = '0/350'; 
    }

    function updateCharCount() {
        const postContent = document.getElementById('postContent').value;
        const charCount = postContent.length;
        document.getElementById('charCount').textContent = `${charCount}/350`;
    }
</script>

</body>
</html>